export interface Chat {
  vpos: number;
  user_id: string;
  message: string;
}

export interface Options {
  font_name: string;
  font_size: number;
  margin: number;
  outline: number;
  displayed_time: number;
}
